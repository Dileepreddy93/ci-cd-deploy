name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v3

    - name: 🟢 Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: 📦 Install dependencies
      run: npm install

    - name: 🧪 Run tests
      run: npm test

    - name: 🏗️ Build project
      run: npm run build

    - name: 🚀 Deploy (Optional - comment this if not needed)
      run: echo "Add your deployment steps here, like rsync, scp, vercel --prod etc."
